# Copy this file to .env and fill in real values before running docker compose.

# Shared secret used to authenticate requests to the VectorMorph server.
BEARER_TOKEN=change-me

# Required by the example client to generate embeddings.
OPENAI_API_KEY=sk-...
